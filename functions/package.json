{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "test:rules": "firebase emulators:exec --project demo-gloows365e-test --only firestore,auth \"jest --config jest.rules.config.js --runInBand\""
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@upstash/redis": "^1.38.0",
    "axios": "^1.16.1",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "@types/jest": "^29.5.14",
    "firebase-functions-test": "^3.4.0",
    "@firebase/rules-unit-testing": "^4.0.1"
  },
  "private": true
}
